version: "3.9"
services:
  db:
    image: postgres:17
    environment:
      - POSTGRES_USER=jobtracker
      - POSTGRES_PASSWORD=jobtracker_pw
      - POSTGRES_DB=jobtracker_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
